# System Health Administration

Authorized admins can open `/system/health` to inspect migrations, checkpoint version, outbox and mail worker state, failed jobs, and storage status. The surface shows operational data only and does not reveal secrets or private payloads.
